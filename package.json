{
  "name": "brightboards",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "BrightBoards · 心灯墙 — 一个让每一个走过这里的人都被看见的留言项目",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "validate": "node scripts/validate.mjs"
  },
  "dependencies": {
    "astro": "^4.16.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
